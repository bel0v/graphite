 version: 2
 jobs:
    build:
      docker:
        - image: circleci/node:10.16
      steps:
        - run:
            name: Install dependencies
            command: npm install
        - run:
            name: Run tests
            command: npm test
